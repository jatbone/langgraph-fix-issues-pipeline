{
  "name": "db-helper",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "tsc",
    "migrate": "npm run build && node dist/migrate.js",
    "insert": "npm run build && node dist/insert.js"
  },
  "dependencies": {
    "@langgraph-fix-issues-pipeline/backend": "workspace:*",
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "@types/node": "^20.11.17",
    "typescript": "^5.3.3"
  }
}
